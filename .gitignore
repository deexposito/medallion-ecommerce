.venv/
__pycache__/
*.pyc

# dbt
target/
dbt_packages/
logs/
profiles.yml
.user.yml

# raw data (not uploaded to GitHub, download instructions in README)
data/raw/*
!data/raw/.gitkeep
*.duckdb

# Power BI - the parquet hand-off is regenerated by scripts/export_gold_to_parquet.py;
# the .pbix itself IS committed once built (Import mode embeds the data, so it's
# self-contained and viewable without re-running the pipeline)
power-bi/data/*.parquet
*.pbix.bak

.env
